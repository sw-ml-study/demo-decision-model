[package]
name = "tdm-trace"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Parsing and validation for the typed decision trace interchange format."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
