Make progress on the sw-atlas problem by answering the question that blocks its Saga 3 step 3: can a PR05 card scorer pick a resource whose card was never in training? Measure it on sw-atlas's own data, read-only from its checkout and never committed here: its built corpus (643 resources with title, aliases, concepts and summary) and its frozen question sets (308 paraphrases written to share no title word or alias with their answer, plus exact-name, ambiguous, follow-up, meta and off-topic). Build the benchmark in demos/atlas/: cards from the corpus, queries from the paraphrase set, resources split into known and held-out so the held-out cards are never offered during training. Train with lib/scorer.mlpl unchanged -- that is also NV01, the test of whether these abstractions are real or ELIZA-shaped, so any change lib/ needs is itself the finding and must be reported. Report a lexical matcher baseline (recall at 1, 5 and 20) beside the scorer's rerank accuracy and MRR, separately for known and held-out cards, and say plainly if the held-out number is bad. Write the result into docs/experiments/ and into docs/reference/downstream-requests.md so sw-atlas can act on it. Close with the full gate, a commit, a push and a summary.