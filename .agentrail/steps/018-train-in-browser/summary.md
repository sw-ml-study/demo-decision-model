In-browser training shipped: scripts/bundle-program flattens an include tree and proves browser-safety by call-graph reachability (not a grep); demos/eliza/browser.mlpl is demo 01's first model sized for WASM (512 slots, 24 dims, 30 steps -> 0.81 val against the shipped 0.879, 1.7 s native); build-site emits pages/demo01-train.mlpl; scripts/check-browser-program re-bundles, runs it with no source dir, and pins the accuracy, wired into the gate. Footer and README link it with the three steps. Confirmed the Live Editor loads a program by file picker (.mlpl/.txt) or paste, Run with Ctrl+Enter -- there is no URL parameter, which is why a downloadable single file is the integration. Commits a6f9358 and 67c2bdb, pushed.