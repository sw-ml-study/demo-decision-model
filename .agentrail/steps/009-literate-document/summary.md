Literate document shipped so the live demo footer link resolves before today's blog post: docs/literate/demo-decision-model.org with a six-block runnable primer (distribution, confidence vs margin, Scale expectation, the dont_know/robots_are slot-986 collision behind 'I don't know' -> COMPUTER, a forward pass by hand, and TL01 in miniature) and every lib/ and demos/eliza source split at function boundaries, tangling byte for byte. scripts/check-tangle in the gate proves parity by awk and org-babel-tangle, reruns primers against recorded results, and checks the HTML export is fresh; the org-babel cross-check exposed a stray double blank line in export.mlpl, fixed. Published as literate.html in pages/ with links to its .org source and GitHub-hosted results/catalog/probe. Live and verified: literate.html 200. Also fact-checked the blog draft (via the user's local preview) and reported seven discrepancies before publication: page size now 629 KB gz not 250 KB, browser latency ~5 us not 1.3 ms, the 1966 rules do not yet label training data and the +0.483 margin is partly by construction, the page does not emit decision-trace-v1, the results row is not auto-written, no probe checks the model-into-grad limitation, and slots are shared not one per word.