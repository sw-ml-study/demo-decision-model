User plan: train a better model from a real ELIZA oracle rather than from frames one person wrote. Implement a faithful ELIZA in MLPL (Weizenbaum's DOCTOR script: keywords with ranks, decomposition patterns, reassembly rules with pronoun reflection, the MEMORY rule, and the opening line) as the labelling oracle -- this subsumes the legacy eliza-engine step. Run it over a large, varied input corpus (movie quotes, first sentences of novels, memes, jargon, slang, everyday situations) and capture, per input, the keyword and decomposition rule that fired and the output. The decision model is then trained to choose the move (which rule family, or recall, or 'none of these'), while ELIZA's deterministic reassembly composes the reply from the user's own words -- so 'I don't have any problems' can get 'Don't you really have any problems?'. Prefer public-domain and self-written sources for the committed corpus (Project Gutenberg first lines, pre-1929 works); do not commit large amounts of copyrighted quotation to the public repository. Close with the full gate, a detailed commit to main, a push, and a summary.