PR05 delivered: lib/scorer.mlpl (question-conditioned card scorer, cosine with a learned scale, mlplunit-tested, demo-neutral), crates/tdm-model/src/scorer.rs taking candidates as text at call time at 1e-9 parity, trained on 8,336 tuples across two questions with 12 rules held out entirely. Generality costs ~1 point where candidates trained (85.3% vs model 4's 86.1% on the same rows); on held-out cards it is 0.4% full field, 36.5% in a five-card warm field, chance against cold ones -- a lower bound in an unfriendly regime, written up in DC01 with the reusable harness shape. New sw-MLPL finding Q5 (record field reads and shape() inside grad) with probe. docs/reference/downstream-requests.md records both sw-atlas asks. Commit 64313f1, pushed.