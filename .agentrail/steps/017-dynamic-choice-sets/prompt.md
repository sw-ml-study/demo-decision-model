Deliver PR05, the lesson sw-atlas is blocked on (docs/plan.md Saga 4 step 1; request PR05 in the sibling sw-atlas docs/demo-decision-model-requests.md): a question-conditioned scorer score_i = f(h_state, h_question, h_choice_i) trained over (state, question, choices, answer) tuples, so the same weights score choices they never saw. Choices are supplied as text at inference, not as head columns. In lib/, demo-neutral, mlplunit-tested, with a Rust forward pass in crates/tdm-model at 1e-9 parity. Measure the thing that matters: hold whole choices out of training entirely and report accuracy on them, against the fixed-head Choice (model 4) on the choices both can answer -- generality is not free, and the price is a measurement. Demo 01 supplies the tuples: the DOCTOR rules as cards (pattern, example reassemblies), with a held-out set of rules the scorer never trained on, including the rare rules model 4 had to merge away. Keep the card width and pooling configurable, since sw-atlas's cards are titles and abstracts rather than short patterns. Write the lesson up in docs/experiments/ with its results row and catalog entry. Close with the full gate, a detailed commit to main, a push, and a summary.