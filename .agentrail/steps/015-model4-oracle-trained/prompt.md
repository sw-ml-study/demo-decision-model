Train model 4 on the oracle corpus: the Choice is over the DOCTOR rule ids the oracle used (rare rules merged into their keyword's catch-all), with the existing Noul heads kept via masked loss on the tagged frame sentences. Featurize in Rust (the corpus is too large for the MLPL interpreter's O(n^2) list building) with an exact vocabulary capped by frequency, and train in MLPL. Snapshots at measured times; default by mean validation over all heads. Evaluate agreement with the oracle on the validation split and on the 96 probes (oracle-labelled), and how often the chosen rule's pattern matches. The page answers with the keyword engine's reassembly for the chosen rule, keeping the repeat rule, deflection and escalation. Keep the page size in check. Close with the full gate, a detailed commit to main, a push, and a summary.