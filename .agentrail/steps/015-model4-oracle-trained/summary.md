Model 4: a Choice over the 35 DOCTOR rules, trained on 7,595 oracle-labelled inputs with masked Noul labels; 84.9% agreement with the oracle on held-out inputs (majority class 30.1%), 93.8% on the 96 probes; page ships it and answers with the chosen rule's own reassembly. Rust featurizer + minibatch Adam in MLPL (1,268 steps/10 min). Vocabulary now always covers the script's own words ('hello' had fallen off the frequency cutoff). Commits b13b1d0 (source) and a7e01c5 (pages), pushed.