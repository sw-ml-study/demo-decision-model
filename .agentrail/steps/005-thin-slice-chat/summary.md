Thin end-to-end slice complete: there is a working demo. 'just chat "my mom never listens to me"' loads committed weights and answers in 0.5-2.1 ms; 'just transcript' runs nine turns in 0.11 s; 'just eliza-train' retrains in 23.9 s and is deterministic (identical file bytes, verified by hash). Built lib/text.mlpl (hashed word+bigram featurizer) and lib/choice_model.mlpl (pooled-embedding Choice; training over globals, inference over a plain weights record, with an exact-parity test between the two forward passes), plus demos/eliza (nine classes, canned replies, the 1966 keyword matcher, corpus frames). Model starts from seeded random init, 33,065 params, trained only on 232 generated sentences, no pretrained vectors. Results SL01: train 1.000/val 0.879/wild 0.750 against matcher 0.526/0.397/0.400, margins +0.474/+0.483/+0.350; validation holds out a whole sentence frame per class. Honest failures recorded prominently: reports 1.000 on most inputs, confidently wrong off-distribution ('i bought a new car' -> DESIRE 0.786; 'i dreamed my mother was a computer' -> DESIRE 0.883 where the matcher is right), val COMPUTER 0.000; calibration columns marked knowingly deferred rather than omitted. New finding Q4: Adam's moment state is process-global, which had inflated a sweep to val 0.948 versus a true 0.879 -- probe and pinned test added, and every future sweep must run one configuration per process (affects CB02, EN01). The no-generation gate is now real: a test walks every class at every turn and requires byte-identical quotation. Marked provisional; PR01 supersedes it. Gate clean: 30/30 MLPL, 18/18 Rust, 4/4 probes. Committed d6b48a1 and pushed.