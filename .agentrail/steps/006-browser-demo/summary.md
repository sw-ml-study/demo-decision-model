Live browser demo shipped: https://sw-ml-study.github.io/demo-decision-model/ . MLPL stays the only trainer; demos/eliza/export.mlpl writes a JSON bundle with weights, featurizer settings, labels, replies, matcher keywords, threshold, examples, and a 319-input parity set (rounding to 1e-6 moved probabilities by at most 4.1e-7 and changed zero decisions). crates/tdm-model ports featurizer, forward pass, matcher and responder to Rust with parity on the first attempt: every probability within 1e-9, matcher identical on all 319 inputs, and a no-generation test in the port. crates/tdm-web is a Yew page with chat, example prompts, a trace toggle (state in with hashed features, choices offered, distribution bars, confidence/margin, policy, verbatim table entry, 1966 matcher agreement), ?say= shareable links with a natively tested parser, and a footer with repo, copyright, licence, literate-document link, and build SHA/host/UTC time. Sizes: 636 KB WASM, 254 KB gzipped incl. the ~150 KB bundle; model 33,065 f64 = 264 KB in memory. Built locally into committed pages/ from a clean commit (build-site marks -dirty otherwise), published unchanged by a GitHub Actions workflow; Pages enabled with build_type=workflow; both push and dispatch runs succeeded; live URL verified rendering in headless Chrome. Not verified: decision timing reads 0.0 us under headless Chrome's virtual clock. Known gap: footer's literate link 404s until the next step. Gate clean: 30/30 MLPL, 28/28 Rust, 4/4 probes.