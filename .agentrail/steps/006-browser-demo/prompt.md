User asked for a demo that needs no clones or builds: run demo 01's trained Choice model in the browser. MLPL stays the only trainer and exports a JSON bundle (weights, labels, featurizer settings, canned replies, matcher keywords, policy threshold, and a parity set of inputs with MLPL-computed probabilities). crates/tdm-model ports the featurizer and forward pass to Rust with a parity test against the MLPL numbers. crates/tdm-web is a Yew/WASM chat page with a trace toggle showing state in, the choices offered, and the distribution as confidence bars, plus the policy and the matcher's pick. No demo identifiers in crates/: every demo string arrives from the bundle. Add a Pages workflow and 'just web'. Report weights and WASM sizes. Close with the full gate, a detailed commit to main, a push, and a summary.