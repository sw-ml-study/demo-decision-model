ELIZA mechanics live (build aa1bca6). The page had no conversation mechanics, which is why 'earlier you said' never appeared. Added a demo-neutral conversation engine in tdm-model driven by fixtures/bundles/demo01-script.json: program-kept memory with content keywords; the user's repeat rule (a content word reused >=2 turns later recalls that statement, reflected, once); Weizenbaum's MEMORY rule for 'my ...' statements when nothing fits; decomposition/reassembly keyed by the model's label, with keyword rules for any label ('I don't have any problems' -> 'Don't you really have any problems?') and a top-guess find rule when the literal keyword agrees with an under-confident model. Fixed three flaws found on a scripted session (memory rule starving recall, filler in quoted memories, immediate repeats). Trace shows mechanism, frame, slots and memory; tests rebuild every reply from frame+slots and pin the session behaviours. Gate clean: 30/30 MLPL, 47/47 Rust, 4/4 probes. Next: Noul heads (question/negative/positive) trained jointly.