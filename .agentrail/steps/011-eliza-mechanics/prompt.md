User: the demo is not ELIZA-like and never says 'earlier you said'. Piece 1 (program mechanics, no retraining): a generic conversation engine in tdm-model driven by a data script (fixtures/bundles/demo01-script.json): per-turn memory with content keywords (stopwords from the script), the user's repeated-keyword recall rule ('Earlier you said ...' with pronoun reflection, each memory recalled once), Weizenbaum's MEMORY rule for 'my ...' statements when nothing fits, and ELIZA decomposition/reassembly keyed by the model's chosen class (wildcard patterns with reflected captures, plus find-a-word rules), so 'I don't have any problems' can get 'Don't you really have any problems?'. Every reply remains a script frame plus the user's own reflected words. Trace shows memory, the rule that fired, and captures. Piece 2 (next step): Noul heads question/negative/positive trained jointly with the Choice. Close with the full gate, a detailed commit to main, a push, and a summary.