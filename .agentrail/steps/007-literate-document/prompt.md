User request: an Emacs org-mode literate, reproducible-research document for the MLPL code, using sw-MLPL's ob-mlpl backend, following ../moe-microscope/docs/literate/moe-microscope.org. A runnable primer of small self-contained MLPL blocks with recorded #+RESULTS beside the measured numbers, then the lib/ and demos/eliza sources split at function boundaries with prose before each, carrying :tangle targets. Add scripts/check-tangle (awk tangle must equal the committed files byte for byte; primer blocks must reproduce their recorded results) and scripts/publish-literate (batch HTML export with no evaluation and no timestamp) writing docs/literate/literate.html, which build-site copies into pages/ so the live demo footer's link resolves. Rebuild and publish pages. Close with the full gate, a detailed commit to main, a push, and a summary.