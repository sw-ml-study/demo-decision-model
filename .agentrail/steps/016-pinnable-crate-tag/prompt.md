Request TAG from sw-atlas (docs/demo-decision-model-requests.md in the sibling software-wrighter-lab/sw-atlas): give them a revision to pin. Verify that crates/tdm-model and crates/tdm-trace build as standalone git dependencies from a clean checkout (a scratch crate outside this repo that depends on them by git rev, builds, parses a committed bundle and validates a trace). Record the crate contract in docs/reference/: which bundle versions the crates read, that the parity tolerance the tests enforce is 1e-9, and the compatibility note that a bundle whose labels are another program's rules carries no keyword matcher, so a reader pinned before that change cannot load model 4. Then tag the revision (annotated, notes carrying those facts), push the tag, and state the pin line sw-atlas should use. Close with the full gate, a commit to main, a push, and a summary.