Saga 1 step 003 complete. schemas/decision-trace-v1.schema.json fixes the trace format before any model exists; its decision shape mirrors lib/decision.mlpl field for field. crates/tdm-trace (first Rust in the repo) parses and validates it, enforcing the two invariants that carry the project's claim: exactly three kinds (a trace claiming 'generate' is rejected), and output text reconstructible from a table the trace itself carries. Design problem found and fixed here rather than in step 005: classic ELIZA splices transformed user text into a frame, so strict byte-identity would have been unimplementable the moment the recall template appeared. The invariant is now reconstruction -- entries may carry {} placeholders, turns record slots, text must equal the entry with slots filled in order, placeholderless entries must still match byte for byte, and slot count must match exactly. Validation is heavy on rejection: selected/confidence/margin re-derived and checked, noul p == probs[1] with labels exactly [false,true], scale expectation == sum i*p_i, kind-specific fields rejected on wrong kinds, turns ordered, ids unique, 256-label ceiling. fixtures/traces/example-turn-v1.json is a hand-written turn exercising all three kinds plus memory plus a dynamic recall choice, with provenance stating the numbers are illustrative and uncitable. Gate grew scripts/check-rust and scripts/check-traces. sw-checklist's real findings fixed (683-line lib.rs split into 7 modules, both oversized functions decomposed); its module-function-count rule conflicts with its crate-module-count rule for an accessor-based interchange crate, and the deviation is documented in docs/implementation/rust-conventions.md. just check passes clean: 16/16 MLPL tests, 15/15 Rust tests, 3/3 probes, 8 checks. Committed 1e4c1b2 and pushed to main.