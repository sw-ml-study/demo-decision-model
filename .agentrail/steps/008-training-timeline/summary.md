Live demo now shows training: one MLPL run from seeded random weights snapshotted after 0/11/22/55/110 steps (0/1/2/5/10 s on M1 Max), each measured identically; picking a snapshot re-decides the whole conversation, beside that snapshot's metrics and a chart of val accuracy vs probe confidence whose caption is computed from the data. Measured (TL01): val 0.069/0.845/0.879/0.879/0.879, wild 0.10/0.80/0.75/0.75/0.75, probe confidence 0.115/0.514/0.740/0.808/0.811 -- learning happens in the first second, accuracy settles at 2 s, after which training only buys confidence including on inputs never trained on; the user's hoped-for 'each snapshot measurably better' is not true and the page says so. Page reframed as 'Typed Decision Model' with ELIZA as demo 01; opening line 'The doctor is in. What seems to be your problem?' from the bundle; ?snap= links. Bundle v2 with per-snapshot parity (within 1e-9 at all five). Fixed a live defect: the '100-run' timing never applied (a string replace silently no-opped after cargo fmt), so the page timed one run while claiming 100; edits are now checked. Page 1.83 MB WASM / 629 KB gz. Deployed from clean commit 46136aa, verified rendering live. Reply quality NOT addressed: still the thin-slice model with no reassembly rules.