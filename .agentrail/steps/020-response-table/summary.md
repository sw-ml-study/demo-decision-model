Closed as subsumed: the reply tables are committed data (fixtures/bundles/demo01-script.json and demo01-doctor.json; model 1's nine-class table is generated in demos/eliza/eliza.mlpl), and the no-generation gate is enforced in both languages -- tests/test_eliza.mlpl walks every class and turn the selector can reach and requires byte-identity with a table entry, while four Rust tests rebuild every reply of a session from its frame and slots. The wording changed deliberately: byte-identity held for model 1 and stopped holding the moment a reply could quote the visitor, so the invariant enforced now is the stronger 'exactly one of the candidates the program offered, rebuildable from exactly those parts'. Recorded in docs/reference/superseded-steps.md (commit fa859a8).