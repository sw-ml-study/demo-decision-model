Saga 1 step 001 complete. lib/decision.mlpl holds the typed decision contract: one Decision constructor setting kind/question/labels/probs/selected/confidence/margin/calibration; choice, noul and scale constructors each with a _tempered variant recording its scalar and a _from_probs variant for trace replay; read-by-name policy accessors; caller-owned act/abstain thresholds; and a validator covering kind, label agreement, non-negativity, normalization, selected range, and expectation-iff-scale. tests/test_decision.mlpl pins field names and arithmetic in 13 tests, including the confidence-vs-margin distinction (.48/.21 vs .48/.01), the Scale expectation separating two distributions with the same mode, dynamic choice sets with NONE, and an assertion that there are exactly three kinds and generate is not one. Gate built: scripts/check runs check-structure, the new check-abstraction (greps lib/, crates/, schemas/ for demo vocabulary and any generation path), check-doc-links, check-catalog, check-mlpl-style, run-tests, run-probes; wired to justfile. Also mlplunit.conf, catalog/lessons.toml, docs skeleton (README, reference/results.md, reference/sw-mlpl-findings.md with the three open questions, implementation/cross-repo-handoffs.md), directory READMEs so demos/probes/fixtures survive a clone, README status refreshed. Deviation: docs/overview/ not created, since docs/plan.md is the overview at the path the user asked for. just check passes clean, 13/13 tests. Committed d0561e8 and pushed to main.