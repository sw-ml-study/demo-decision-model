Saga 1, step 1: repository gate and the decision contract. Create justfile, mlplunit.conf, scripts/ (run-tests, check-mlpl-style), catalog/lessons.toml, and the docs skeleton (docs/README.md, docs/overview/, docs/reference/results.md, docs/reference/sw-mlpl-findings.md, docs/implementation/cross-repo-handoffs.md). Then write lib/decision.mlpl: the Decision record and the choice/noul/scale signatures, with confidence, margin and expectation defined once and pinned by mlplunit tests so later sagas cannot drift them. lib/ must contain no ELIZA identifier. Add the abstraction-gate script that enforces that. See docs/plan.md 'Saga 1' and 'The architecture rule'. Close the step with the full gate, a detailed commit to main, a push, and a summary.