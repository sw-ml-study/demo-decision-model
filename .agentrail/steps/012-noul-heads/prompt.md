User request: ask the model several questions per input -- is the user asking a question, being negative, being positive -- as Noul decisions. Add three sigmoid Noul heads on the shared encoder, trained jointly with the Choice (plan PR02/PR04: one forward pass, many typed decisions), with per-frame question/sentiment annotations in the v2 corpus and a question token for '?'. Freeze Noul labels for the 96 probes first. Use the Nouls in the conversation policy when the Choice escalates (question -> 'Why do you ask?' family, negative -> 'You seem upset...', positive -> 'That sounds good...'), show them as bars in the trace, measure each Noul on the probes, re-export the bundle, deploy. Close with the full gate, a detailed commit to main, a push, and a summary.