Implement the 'hello world' of this repository, from docs/research2.txt: a concise, idiomatic typed-decision demo in sw-MLPL as an Emacs org-mode literate document, in four stages that build on each other -- (1) pure math: a typed decision is a softmax over a bounded domain, no learned model; (2) a learned Choice over a tiny corpus, so the probabilities come from training rather than invention; (3) one shared state feeding three typed heads (Choice, Noul, Scale), which is the architecture in three lines; (4) calibration: reliability buckets, Brier and ECE before and after temperature scaling, which is the stage the 25-line Python version cannot honestly claim. Domain is message triage (legitimate/spam/phishing), not ELIZA, so it doubles as evidence that lib/ is not ELIZA-shaped. Call it typed-decisions, never jev: Jev's architecture and RLCD training are unpublished and this is a Jev-like interface demonstration only. Tangle the program from the org document and make the gate check it: generalize scripts/check-tangle and scripts/publish-literate to every docs/literate/*.org, run the tangled program, and pin its numbers. Bundle it for the Live Editor too if it stays browser-safe. Link it from the README and docs index, with a results row and a catalog entry. Close with the full gate, a detailed commit to main, a push, and a summary.