Model 2 live (build c8269c6). Diagnosed the user's two complaints by per-feature blame: half hash collisions, half spurious cues in the author's own frames ('you' -> COMPUTER, 'friend' -> GREET). Froze hand labels for the 96 probes (17 classes) before writing any v2 frame. Built an exact vocabulary (1,173 features; unknown words contribute nothing), word dropout, label smoothing, 17 classes with a trained NONE escape hatch, and a three-way policy outcome (act / none applies / escalate) implementing the user's 'need more info' idea. Training-budget comparison 10 s / 1 min / 5 min / 10 min: default 5 min chosen by validation (0.884); 10 min degrades. What visitors see on the 96 probes: reply class correct 69% vs 40%, right when not escalating 78% vs 38%, wrong machine replies 1 vs 12, wrong greetings 0 vs 7, escalated 33% (28 of 32 escalations were cases the model would have got wrong). Strict on 78 never-trained probes: 62% vs 38%, confidence when wrong 0.36 vs 0.72. Ratchet test pins the two complaints; parity holds at all five snapshots. Limits: a third escalate to a flat reply in the browser; all labels/frames/yardstick are the author's (oracle corpus is the remedy); page 865 KB gz.