Answer the apples-to-oranges criticism with a measurement instead of prose. Run the same 24 held-out messages that typed-decisions.mlpl is scored on through a small local LLM via Ollama, using the same bounded-choice method the HN post uses and this repository's own rule: first-token top_logprobs renormalized over the legal answer set, no generation. Report accuracy, Brier, ECE and mean confidence for both, plus what each costs in parameters, bytes and latency per decision. If the LLM wins on accuracy, say so first and plainly. Commit the harness, the raw per-message results, a results row, and rewrite the comparison sections of both literate documents around the measured numbers rather than around line counts. Close with the full gate, a commit, a push and a summary.