Settled the apples-to-oranges criticism by measurement. scripts/llm-baseline runs the same 24 held-out messages typed-decisions.mlpl is scored on through local models by the same bounded-choice method (first-token top_logprobs renormalized over the legal answers, nothing generated), five arms, every per-message row committed to demos/typed-decisions/llm-baseline.tsv. Result: llama3.2:3b zero-shot 0.792 and gemma4:31b 1.000 against this repository's 0.750, so a 3B model beats the trained model untrained and a 31B gets every message right; qwen3:0.6b gets 0.458. Two secondary findings matter more: six in-prompt examples pushed qwen3:0.6b's first-token mass off the legal answer set (0.34 legal mass), the exact objection raised under the original post and one a fixed three-column head cannot have; and llama3.2:3b is better calibrated untouched (ECE 0.006) than ours after temperature scaling (0.097). Both literate documents and the README now lead with the loss, and conclude that spam/phishing was the wrong task to prove anything with: a trained TDM earns its place where no pretrained model has the knowledge, as model 4 does over the 35 DOCTOR rules. LB01 catalog entry and results row. Commits 314e39f and d2be4a7, pushed.