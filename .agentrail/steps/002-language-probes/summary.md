Saga 1 step 002 complete. Verified all three sw-MLPL capability questions in-repo rather than taking the sibling repo's answers on trust; all three green. Q1: freeze holds a frozen encoder at exactly 0 delta while the head trains 0.495 (test asserts the head moved so the check is not vacuous); caveat found that freeze is a statement, not an expression, so 'fe = freeze(m)' leaves fe undefined. Q2: a bilinear scorer f(h_state,h_choice)=h_s W h_c^T differentiates to exactly the outer product, total absolute error 0, at PR05's real rank-2 shape rather than the scalar form; the dynamic-choice-set primitive will train. Q3: experiment-wrapped train loops run inside user functions, with two constraints found here: the experiment name is a string literal not an expression, so runs are told apart by recording the swept setting as its own *_metric, which experiment_metric returns as aligned columns in run order. Also verified mlpl-repl exits 1 on an evaluation error, so scripts/run-probes is a real gate. Three standalone probes under probes/, three pinned assertions in tests/test_capability_probes.mlpl, findings document rewritten with answers, caveats, workarounds and the gate note. No blockers, no work order open. just check passes clean: 16/16 tests, 3/3 probes. Committed c8ed429 and pushed to main.