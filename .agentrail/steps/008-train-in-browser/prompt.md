User request: demo training in the browser without dragging sw-MLPL into this repo. Produce a self-contained, browser-safe training program (the include tree inlined by a bundler like ../moe-microscope/scripts/bundle-program, no file IO, no clock_ms) that runs in sw-MLPL's own hosted Live Editor, which loads and interprets a self-contained .mlpl file. First confirm exactly how the Live Editor loads a program (URL, paste, file), and whether the ~23 s native training is tolerable interpreted in WASM; if not, shrink the demo configuration and say so. Link it from the live demo and the README. Close with the full gate, a detailed commit to main, a push, and a summary.